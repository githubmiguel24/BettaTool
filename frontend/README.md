# bettafish-frontend

React + Vite + Tailwind dashboard for the Betta morphometrics thesis
system. See the repo root `README.md` for how this fits together with
`../backend/` — short version: this UI is currently a static shell with no
backend integration (no `fetch`/`axios` calls anywhere in `src/`).

```bash
npm install
npm run dev
```
